# 산업 현장 안전 감지 로봇 — 세부 프로젝트 기획서

> **과정명** AI 로봇 SW 개발자 교육과정
> **프로젝트명** On-Device AI 기반 산업 현장 안전 감지 로봇
> **플랫폼** Raspberry Pi 5 + Camera Module
> **작성일** 2026-05-11
> **버전** v4.1

---

## 변경 이력

| 버전 | 주요 변경 내용 |
|------|---------------|
| v3.0 | 최초 작성 (라인 트레이서 기반) |
| v4.0 | 라인 센서 제거 → 카메라 기반 자율 이동 전환 / 디렉토리 간략화 / PPE·위험 표지 모델을 Object Detection 방식으로 변경 |
| v4.1 | 초음파 센서 제거 (추가 센서 없이 카메라만 사용) / 모터 제어 라이브러리 gpiozero 채택 / MOVE_TO 도착 판정 및 고착 감지 로직 명확화 |

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기능 정의](#2-기능-정의)
3. [하드웨어 구성 및 스펙](#3-하드웨어-구성-및-스펙)
4. [SW 아키텍처](#4-sw-아키텍처)
5. [세부 기능 명세](#5-세부-기능-명세)
6. [AI 모델 설계](#6-ai-모델-설계)
7. [데이터셋 전략](#7-데이터셋-전략)
8. [개발 환경 및 기술 스택](#8-개발-환경-및-기술-스택)
9. [시스템 흐름도](#9-시스템-흐름도)
10. [개발 일정](#10-개발-일정)
11. [리스크 및 대응 방안](#11-리스크-및-대응-방안)
12. [성능 지표 (KPI)](#12-성능-지표-kpi)

---

## 1. 프로젝트 개요

### 1.1 배경 및 목적

산업 현장에서 안전사고는 매년 800명 이상의 사망자를 낳고 있으며, 사람 감독자만으로는 24시간·전 구역의 안전을 보장하기 어렵다. 본 프로젝트는 카메라로 현장을 스캔하며 자율 순찰하는 로봇에 On-Device AI를 탑재하여, 사람 감지 시 해당 대상에게 접근 후 안전 장구 착용 여부와 위험 구역 근접 상황을 판별하고 즉각 경고를 발령하는 시스템을 구축한다.

### 1.2 핵심 가치

| 가치 | 설명 |
|------|------|
| **On-Device 추론** | 네트워크 단절 환경에서도 안전 판단 중단 없음 |
| **자율 순찰** | 제자리 회전으로 주변을 스캔하고 감지 대상을 향해 자율 이동 |
| **자동 접근·판별** | 사람 감지 시 접근 후 AI로 위험 여부 분석 |
| **즉각 경고** | 위험 감지 후 2초 이내 음성·LED 경고 출력 |
| **2주 완성** | 핵심 기능에 집중한 현실적인 개발 범위 |

### 1.3 로봇 동작 컨셉 — 상태 전이 모델

```
┌────────────────────────────────────────────────────────────┐
│                      동작 상태 전이                         │
│                                                            │
│   ┌──────────┐  사람 감지   ┌──────────┐                   │
│   │  SEARCH  │ ──────────► │  WATCH   │                   │
│   │ (제자리  │ ◄────────── │ (정지 +  │                   │
│   │  회전)   │ 사람 소실    │  AI분석) │                   │
│   └──────────┘ or 타임아웃 └────┬─────┘                   │
│        ▲                        │ 위험 판정               │
│        │                        ▼                         │
│        │                 ┌────────────┐                   │
│        │                 │ALERT_EVENT │                   │
│        │                 │ (위반 확정)│                   │
│        │                 └─────┬──────┘                   │
│        │                       │                          │
│        │                       ▼                          │
│        │◄────타임아웃(20s)──┌────────────┐                 │
│        │◄────고착 감지──────│  MOVE_TO   │                 │
│        │                   │ (대상 접근)│                  │
│        │                   └─────┬──────┘                 │
│        │                         │ 도착                   │
│        │                         ▼                        │
│        │                   ┌────────────┐                 │
│        │                   │   ALERT    │                 │
│        │                   │(음성+LED)  │                 │
│        │                   └─────┬──────┘                 │
│        │                         │ 30초 후                │
│        └─────────────────────────┘                        │
└────────────────────────────────────────────────────────────┘
```

### 1.4 상태별 정의 및 타임아웃

| 상태 | 정의 | 타임아웃 | 타임아웃 시 동작 |
|------|------|---------|----------------|
| SEARCH | 제자리 회전하며 카메라로 사람 탐색 | 없음 | — |
| WATCH | 사람 감지 후 정지, PPE·위험 표지 AI 분석 | 15초 | → SEARCH |
| ALERT_EVENT | 위반 유형 확정, MOVE_TO 진입 준비 | 없음 (즉시 전이) | — |
| MOVE_TO | 감지 대상을 향해 자율 이동 | 20초 | → SEARCH |
| ALERT | 음성·LED 경고 발령 | 30초 | → SEARCH |

### 1.5 프로젝트 범위

**포함 (In-Scope)**

- 카메라 기반 제자리 회전 탐색
- 사람 감지 후 대상 방향 자율 이동 (카메라 bbox 기반, 추가 센서 없음)
- PPE(안전모·안전조끼) 착용 여부 판별 — Object Detection
- 위험 표지 인식 및 사람 근접 판정 — Object Detection
- 음성·LED 경고 출력
- 이벤트 로그 및 스냅샷 저장

**미포함 (Out-of-Scope)**

- 지도 기반 경로 계획 (SLAM)
- 장애물 자동 회피 (추가 센서 없음, 타임아웃·고착 감지로 대응)
- 클라우드 연동 대시보드
- 사람 신원 인식
- 경고 후 원위치 복귀

---

## 2. 기능 정의

### 2.1 기능 목록

| 구분 | ID | 기능명 | 우선순위 |
|------|----|--------|----------|
| 주행 | F-01 | 제자리 회전 탐색 (SEARCH) | Must |
| 감지 | F-02 | 실시간 사람 감지 | Must |
| 주행 | F-03 | 감지 대상 자율 이동 (MOVE_TO) | Must |
| 감지 | F-04 | 안전모 착용 여부 판별 | Must |
| 감지 | F-05 | 안전조끼 착용 여부 판별 | Must |
| 감지 | F-06 | 위험 표지 인식 + 근접 판정 | Must |
| 경고 | F-07 | 음성 경고 출력 | Must |
| 경고 | F-08 | LED 경광등 점멸 | Must |
| 로그 | F-09 | 이벤트 로그 + 스냅샷 저장 | Must |
| 표시 | F-10 | 탑재 디스플레이 경고 메시지 | Could |

### 2.2 우선순위 정의

- **Must**: 2주 내 반드시 구현, 데모에서 동작해야 함
- **Should**: 시간 여유 시 구현
- **Could**: 추가 구현 (여유분)

---

## 3. 하드웨어 구성 및 스펙

### 3.1 하드웨어 구성도

```
┌─────────────────────────────────────────────────────────┐
│                       로봇 본체                          │
│                                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │               Raspberry Pi 5                     │   │
│  │           (메인 컨트롤 / AI 추론)                 │   │
│  └──────┬──────────┬──────────┬─────────────────────┘   │
│         │          │          │                         │
│  ┌──────▼──────┐   │   ┌──────▼──────┐  ┌───────────┐  │
│  │ Camera      │   │   │ Motor Driver│  │ LED 경광등 │  │
│  │ Module 3    │   │   │  (L298N)    │  │ (GPIO PWM) │  │
│  │ (MIPI CSI)  │   │   └──────┬──────┘  └───────────┘  │
│  └─────────────┘   │          │                         │
│                    │   ┌──────▼──────┐  ┌───────────┐  │
│                    └──►│ DC Motor ×2 │  │ USB 스피커 │  │
│                        │ + 바퀴(2WD) │  │ (음성 경고)│  │
│                        └─────────────┘  └───────────┘  │
│  ┌──────────────────┐   ┌──────────────────────────┐    │
│  │ Pi 보조배터리    │   │ 모터 전원 (6V AA Pack)   │    │
│  │ 10,000mAh (USB-C)│   │ Pi 전원과 반드시 분리     │    │
│  └──────────────────┘   └──────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

**신호 흐름**

```
Camera Module ──CSI──► Pi5 ──TFLite 추론──► 이벤트 판정
Pi5 ──PWM────────────────────────────────► Motor Driver ──► DC Motor
Pi5 ──GPIO───────────────────────────────► LED / Speaker
```

### 3.2 부품 스펙

#### 메인 컴퓨팅

| 항목 | 사양 |
|------|------|
| 보드 | Raspberry Pi 5 (4GB 이상 권장) |
| CPU | Cortex-A76 × 4코어 @ 2.4GHz |
| 저장장치 | MicroSD 32GB 이상 Class10 |
| OS | Raspberry Pi OS 64-bit (Bookworm) |

#### 카메라

| 항목 | 사양 |
|------|------|
| 모델 | Raspberry Pi Camera Module 3 |
| 캡처 해상도 | 320×240 (30fps) |
| 추론 주기 | 3~5fps |
| 인터페이스 | MIPI CSI-2 |
| 설치 위치 | 전방 상단, 수평 기준 약 15° 하향 |

> 캡처 해상도를 320×240으로 낮춰 Pi5 처리 부담을 줄인다. 각 모델 입력 크기에 맞게 추론 시 별도 리사이즈를 적용한다.

#### 모터 & 구동계

| 항목 | 사양 |
|------|------|
| 모터 드라이버 | L298N Dual H-Bridge |
| DC 모터 | 6V TT 기어모터 × 2 |
| 바퀴 구성 | 2WD + 캐스터 휠 |
| 제어 라이브러리 | gpiozero (Motor, PWMOutputDevice) |

**GPIO 핀 배정 (BCM 기준)**

| GPIO | 용도 |
|------|------|
| 17 | 좌 모터 전진 (IN1) |
| 27 | 좌 모터 후진 (IN2) |
| 18 | 좌 모터 PWM |
| 22 | 우 모터 전진 (IN1) |
| 23 | 우 모터 후진 (IN2) |
| 13 | 우 모터 PWM |
| 12 | LED 경광등 PWM |

**속도 설정값**

| 동작 | PWM 값 | 비고 |
|------|--------|------|
| SEARCH 회전 | 0.25 | 좌 전진 + 우 후진 (제자리 회전) |
| MOVE_TO 직진 | 0.40 | 대상 방향 전진 |
| MOVE_TO 조향 | 0.32 | 좌우 조향 시 |

**모터 명령 중복 방지**: 이전 명령과 동일한 명령이 들어올 경우 모터에 재전송하지 않는다. 직전 명령 상태를 캐싱하여 변경이 있을 때만 GPIO에 전달한다.

#### 전원

| 항목 | 사양 |
|------|------|
| Pi 전원 | 10,000mAh USB-C 보조 배터리 |
| 모터 전원 | AA 배터리 팩 × 4 (6V), Pi 전원과 분리 필수 |

---

## 4. SW 아키텍처

### 4.1 레이어 구조

```
╔═══════════════════════════════════════════════════╗
║         Application Layer  (main.py)              ║
║  상태머신 + 타임아웃 + 경고 정책 통합 관리         ║
╠═══════════════════════════════════════════════════╣
║         AI Inference Layer  (detector.py)         ║
║  Person Detect / PPE Detect / Danger Detect       ║
║  상태에 따라 조건부 실행                           ║
║            camera.py (picamera2 스트림)            ║
╠═══════════════════════════════════════════════════╣
║         Hardware Abstraction Layer                ║
║   motor.py                  output.py             ║
║   모터 PWM + 이동 로직       LED + 스피커 통합     ║
╚═══════════════════════════════════════════════════╝
```

### 4.2 디렉토리 구조

```
safety_robot/
├── main.py              # 진입점 + 상태머신 + 경고 정책
├── config.yaml          # 임계값, 핀 번호 등 전체 설정
│
├── hardware/
│   ├── motor.py         # 모터 PWM 제어 + 자율 이동 로직
│   └── output.py        # LED + 스피커 통합 경고 출력
│
├── vision/
│   ├── camera.py        # picamera2 캡처 스트림
│   └── detector.py      # Person / PPE / Danger 통합 파이프라인
│
├── logger.py            # 이벤트 JSON 로그 + 스냅샷 저장
│
├── models/              # Pi5 탑재용 TFLite 모델 (추론 전용)
│   ├── person_detect.tflite
│   ├── ppe_detect.tflite
│   └── danger_detect.tflite
│
├── assets/              # 경고음 WAV
│   ├── no_helmet_ko.wav
│   ├── no_vest_ko.wav
│   └── danger_zone_ko.wav
│
├── training/            # 학습 코드 (개발 PC / Colab 전용, Pi5 미탑재)
│   ├── train_ppe.py
│   ├── train_danger.py
│   ├── convert_tflite.py
│   └── dataset/
│       ├── ppe/         # images/ + labels/ (YOLO 형식)
│       └── danger/      # images/ + labels/ (YOLO 형식)
│
├── logs/                # 런타임 생성
│   └── snapshots/
│
└── tests/
    ├── test_detector.py
    └── test_motor.py
```

> `training/` 폴더는 Pi5에 올리지 않는다. Colab에서 학습 완료 후 생성된 `.tflite` 파일만 `models/`에 복사하여 사용한다.

### 4.3 스레드 모델

| 스레드 | 역할 | 실행 주기 |
|--------|------|----------|
| Main Thread | 상태머신 + 타임아웃 관리 + 정책 판단 | 이벤트 기반 |
| Thread 1: Camera | 프레임 캡처 → 공유 버퍼 | 30fps |
| Thread 2: AI Inference | 상태별 조건부 모델 실행 → Main 큐 전달 | 3~5fps |
| Thread 3: Motor Control | 상태에 따른 모터 명령 실행 | 100ms 주기 |
| Thread 4: Logger | 이벤트 발생 시 JSON + 스냅샷 저장 | 이벤트 기반 |

---

## 5. 세부 기능 명세

### F-01 제자리 회전 탐색 (SEARCH)

| 항목 | 내용 |
|------|------|
| 동작 | 좌 모터 전진 + 우 모터 후진으로 제자리 회전 |
| 회전 속도 | PWM 0.25 |
| 전환 조건 | 2프레임 연속 사람 감지 → WATCH |
| 유지 조건 | 사람 미감지 동안 계속 회전 |
| LED | 녹색 상시 점등 |

---

### F-02 실시간 사람 감지

| 항목 | 내용 |
|------|------|
| 모델 | YOLOv5n TFLite (COCO 사전학습, 직접 학습 불필요) |
| 입력 크기 | 320×320 |
| 신뢰도 임계값 | 0.5 이상 |
| 실행 조건 | 모든 상태에서 상시 실행 |
| 감지 확정 | 2프레임 연속 감지 시 WATCH 전환 |
| 감지 해제 | 5프레임 연속 미감지 시 SEARCH 복귀 |
| 출력 | 사람 바운딩 박스 목록 (x1, y1, x2, y2, confidence) |
| 다중 인물 처리 | 가장 큰 bbox (가장 가까운 인물)를 우선 타깃으로 선정 |

---

### F-03 감지 대상 자율 이동 (MOVE_TO)

ALERT_EVENT 확정 후 감지된 사람을 향해 카메라 bbox 정보만으로 이동한다. 추가 센서 없이 YOLO person bbox의 위치와 크기를 이동 제어의 유일한 입력으로 사용한다.

#### 조향 로직

bbox 중심 X 좌표와 프레임 중심 X 좌표의 차이(오차)를 기준으로 조향 방향을 결정한다.

| 조건 | 동작 | 속도 |
|------|------|------|
| 오차 > +50px (오른쪽) | TURN_RIGHT | 0.32 |
| 오차 < −50px (왼쪽) | TURN_LEFT | 0.32 |
| 오차 ±50px 이내 | FORWARD | 0.40 |

#### 도착 판정

person bbox 면적이 전체 프레임 면적의 15% 이상이 되면 충분히 근접한 것으로 판정, 정지 후 ALERT로 전환한다.

> bbox 높이 비율 대신 면적 비율을 사용하는 이유: 카메라와 사람의 상대 각도가 틀어질 경우 높이만으로 판정 시 오차가 생길 수 있어, 가로·세로를 모두 반영하는 면적 비율이 더 안정적이다.

#### 고착 감지

전진 명령 중에도 bbox 면적이 일정 시간 이상 증가하지 않으면 장애물 등으로 이동이 불가한 고착 상태로 판단하여 타임아웃(20초)을 기다리지 않고 즉시 SEARCH로 전환한다.

| 항목 | 내용 |
|------|------|
| 판정 조건 | FORWARD 명령 중 + 연속 15프레임(약 1.5초) 동안 bbox 면적 변화량 < 500px² |
| 판정 시 동작 | 즉시 SEARCH 전환 |
| 타임아웃과의 관계 | 고착 감지는 타임아웃(20초)보다 먼저 작동하는 조기 탈출 메커니즘 |

#### 사람 소실 처리 (장애물로 인한 시야 차단 포함)

MOVE_TO 중 장애물이 카메라 시야를 가리거나 대상이 실제로 이탈하여 person bbox가 사라지는 경우, 고착 감지가 아닌 "5프레임 미감지" 조건으로 처리된다. 고착 감지는 사람이 보이는 상태에서만 동작한다.

| 상황 | 감지 경로 | 동작 |
|------|---------|------|
| 사람이 보이는데 이동 불가 | 고착 감지 (약 1.5초) | → SEARCH |
| 장애물이 사람을 가림 | 5프레임 미감지 (약 1.5초) | → SEARCH |
| 사람이 스스로 이탈 | 5프레임 미감지 (약 1.5초) | → SEARCH |
| 사람 소실 + 고착 모두 발생 안 할 때 | 타임아웃 (20초) | → SEARCH |

#### 장애물 충돌 대응 요약

추가 센서가 없으므로 충돌을 사전에 감지할 수 없다. 아래 두 메커니즘으로 고착 상태를 탈출한다. 데모 환경에서는 이동 경로 내 장애물을 제거하여 충돌 상황을 최소화한다.

| 메커니즘 | 발동 조건 | 반응 시간 |
|---------|---------|---------|
| 고착 감지 | 사람이 보이지만 접근 안 됨 | 약 1.5초 |
| 타임아웃 | 사람도 소실되어 고착 감지도 안 됨 | 최대 20초 |

---

### F-04 / F-05 안전모·안전조끼 착용 여부 판별

WATCH 상태에서만 실행된다. 전체 프레임을 입력으로 받아 PPE 착용 여부를 Object Detection 방식으로 감지한다.

#### 모델 출력 클래스

| 클래스 | 의미 |
|--------|------|
| `helmet` | 안전모 착용 중인 머리 영역 bbox |
| `no-helmet` | 안전모 미착용 머리 영역 bbox |
| `vest` | 안전조끼 착용 중인 상체 영역 bbox |
| `no-vest` | 안전조끼 미착용 상체 영역 bbox |

#### 착용 판정 방식

PPE bbox가 person bbox 내에 50% 이상 포함되어 있을 경우 해당 인물의 착용 여부로 귀속한다.

| 항목 | 내용 |
|------|------|
| 귀속 조건 | PPE bbox 면적 중 person bbox 내 포함 비율 ≥ 50% |
| 위반 판정 | `no-helmet` 또는 `no-vest` bbox가 person bbox와 위 조건으로 귀속될 때 |
| 위반 확정 | 3프레임 연속 위반 감지 시 ALERT_EVENT 전환 |
| 매 프레임 재확인 | 착용 해제 가능성이 있으므로 캐시 없이 매 프레임 실행 |

#### 복합 위반 처리

안전모와 조끼 동시 미착용 시 ALERT_EVENT는 한 번만 발생하고 위반 유형을 목록으로 함께 전달한다. 경고 음성은 안전모 → 조끼 순 순차 재생한다.

---

### F-06 위험 표지 인식 및 근접 판정

WATCH 상태에서만 실행된다. 전체 프레임에서 위험 표지를 감지하고 person bbox와의 거리를 계산하여 위험 구역 근접 여부를 판정한다.

#### 근접 판정 방식

픽셀 거리를 person bbox 높이로 정규화하여 카메라~피사체 거리에 따른 스케일 차이를 보정한다.

| 항목 | 내용 |
|------|------|
| 거리 계산 | person bbox 중심점과 위험 표지 bbox 중심점 간 유클리드 픽셀 거리 |
| 정규화 방식 | 픽셀 거리 ÷ person bbox 높이 |
| 근접 임계값 | 정규화 거리 < 2.0 |
| 판정 보류 조건 | person bbox 높이가 프레임 높이의 15% 미만 (너무 멀어 신뢰 불가) |
| 위반 시 | DANGER_ZONE_ALERT → ALERT_EVENT 전환 |

---

### F-07 음성 경고 출력

| 이벤트 | 파일 | 내용 |
|--------|------|------|
| 안전모 미착용 | `no_helmet_ko.wav` | "안전모를 착용하세요" |
| 안전조끼 미착용 | `no_vest_ko.wav` | "안전조끼를 착용하세요" |
| 위험 구역 근접 | `danger_zone_ko.wav` | "위험 구역입니다. 즉시 대피하세요" |

| 항목 | 내용 |
|------|------|
| 재생 방식 | pygame.mixer 또는 aplay |
| 복합 위반 | 안전모 → 조끼 순 순차 재생 |
| 경고 지속 | ALERT 상태 30초 유지 후 자동 SEARCH 복귀 |

---

### F-08 LED 경광등 점멸

| 상태 | 색상 | 점멸 패턴 |
|------|------|---------|
| SEARCH | 녹색 | 상시 점등 |
| WATCH | 황색 | 천천히 점멸 (1Hz) |
| MOVE_TO | 황색 | 빠른 점멸 (2Hz) |
| ALERT — PPE 위반 | 황색 | 고속 점멸 (3Hz) |
| ALERT — 위험 구역 | 적색 | 고속 점멸 (4Hz) |

---

### F-09 이벤트 로그 + 스냅샷 저장

| 항목 | 내용 |
|------|------|
| 로그 경로 | `logs/events_YYYYMMDD.json` |
| 스냅샷 경로 | `logs/snapshots/YYYYMMDD_HHMMSS_<event>.jpg` |
| 저장 시점 | ALERT_EVENT 발생 시 |
| 로그 필드 | timestamp, event_type, detail (위반 항목), confidence, snapshot_path |

---

## 6. AI 모델 설계

### 6.1 모델 구성 요약

| # | 파일명 | 기반 | 입력 크기 | 출력 | 직접 학습 |
|---|--------|------|----------|------|----------|
| 1 | `person_detect.tflite` | YOLOv5n (COCO 사전학습) | 320×320 | 사람 bbox | 불필요 |
| 2 | `ppe_detect.tflite` | YOLO 형식 Object Detection | 640×480 | helmet / no-helmet / vest / no-vest bbox | 필요 |
| 3 | `danger_detect.tflite` | YOLO 형식 Object Detection | 640×480 | 위험 표지 bbox | 필요 |

### 6.2 추론 파이프라인

```
카메라 프레임 (320×240 캡처)
         │
         ▼
[Model 1] Person Detector — 모든 상태에서 상시 실행
  출력: 사람 bbox 목록
         │
         │ WATCH 상태일 때만 아래 실행
    ┌────┴────────────────────────┐
    ▼                             ▼
[Model 2] PPE Detector       [Model 3] Danger Detector
  입력: 전체 프레임 640×480     입력: 전체 프레임 640×480
  출력: PPE 클래스 bbox 목록    출력: 위험 표지 bbox 목록
    │                             │
  IoU로 person bbox와 대조     정규화 거리로 근접 판정
    │                             │
    └──────────┬──────────────────┘
               ▼
       위반 이벤트 생성
  PPE_VIOLATION / DANGER_ZONE_ALERT
               │
               ▼
         main.py 상태머신
```

### 6.3 상태별 모델 실행 조건

| 상태 | Person Detect | PPE Detect | Danger Detect |
|------|:---:|:---:|:---:|
| SEARCH | ✓ | ✕ | ✕ |
| WATCH | ✓ | ✓ | ✓ |
| MOVE_TO | ✓ (추적용) | ✕ | ✕ |
| ALERT | ✓ (유지용) | ✕ | ✕ |

PPE 및 Danger 모델은 WATCH 상태에서만 실행하여 Pi5 추론 부담을 최소화한다.

### 6.4 추론 속도 목표

| 상태 | 실행 모델 | 목표 추론 시간 | 목표 fps |
|------|---------|-------------|---------|
| SEARCH | Person only | ≤ 150ms | ≥ 6fps |
| WATCH | Person + PPE + Danger | ≤ 450ms | ≥ 2fps |

목표 미달 시 대응: 입력 해상도 축소 또는 WATCH 상태 PPE/Danger 추론 주기를 격프레임으로 변경하여 재실측한다.

### 6.5 모델 변환 흐름

```
[Colab] 데이터셋으로 모델 학습 → .keras 저장
    ↓
[Colab] TFLite INT8 양자화 변환 → .tflite 생성
        (캘리브레이션 이미지 약 100장 필요 — 검증셋에서 샘플링)
    ↓
[scp] .tflite 파일만 Pi5의 models/ 로 복사
    ↓
[Pi5] detector.py 에서 로드 후 추론만 실행
```

---

## 7. 데이터셋 전략

### 7.1 PPE 데이터셋

Object Detection 형식(이미지 + YOLO .txt 라벨)을 사용한다. 각 `.txt` 파일에는 이미지 내 모든 객체의 클래스 ID와 중심 좌표·크기가 0~1 정규화 비율로 기록된다.

**필요 클래스**: `helmet`, `no-helmet`, `vest`, `no-vest` 4개. 다운로드한 데이터셋의 클래스 구성이 다를 경우 라벨 재매핑 전처리가 필요하다.

| 출처 | 클래스 구성 | 예상 수량 |
|------|-----------|---------|
| Kaggle — HardHat-Vest Dataset v3 | helmet, vest, person | ~5,000장 |
| Kaggle — Construction Site Safety | Hardhat, NO-Hardhat, Safety Vest, NO-Safety Vest | ~2,800장 |
| Roboflow Universe (PPE Detection) | helmet, no-helmet, vest, no-vest | ~3,000장 |
| 자체 촬영 (교내, 다양한 조명·각도) | 동일 | ~300장 |
| **합계** | | **~11,100장** |

### 7.2 위험 표지 데이터셋

| 출처 | 예상 수량 |
|------|---------|
| Roboflow — Safety Sign Detection | ~2,000장 |
| 자체 촬영 (실습실 내 표지판) | ~100장 |

### 7.3 데이터 증강 전략

| 기법 | 목적 |
|------|------|
| 밝기 조정 (±30%) | 조명 환경 다양화 |
| 수평 플립 | 좌우 대칭 다양성 확보 |
| 회전 (±15°) | 카메라 각도 대응 |
| 가우시안 블러 (σ=1~2) | 거리·흔들림 대응 |
| HSV 색상 지터 | 조끼 색상 편향 방지 |

### 7.4 클래스 불균형 대응

`no-helmet`, `no-vest` 클래스는 데이터가 적을 가능성이 높다. 오버샘플링을 적용하여 클래스 간 샘플 비율이 최대 3:7을 넘지 않도록 맞춘다.

---

## 8. 개발 환경 및 기술 스택

| 계층 | 기술 |
|------|------|
| OS | Raspberry Pi OS 64-bit (Bookworm) |
| 언어 | Python 3.11 |
| AI 추론 (Pi5) | tflite-runtime |
| AI 학습 (Colab) | TensorFlow / Keras, Ultralytics YOLO |
| 컴퓨터 비전 | OpenCV 4.x (opencv-python-headless) |
| 카메라 | picamera2 |
| 모터·GPIO 제어 | gpiozero (Motor, PWMOutputDevice) |
| 음성 출력 | pygame.mixer |
| 버전 관리 | Git + GitHub |

**개발 환경 역할 분리**

| 작업 | 환경 |
|------|------|
| 데이터셋 수집·전처리·증강 | 개발 PC / Colab |
| 모델 학습 (PPE, Danger Detection) | Colab (GPU) |
| TFLite INT8 변환 | Colab |
| On-Device 추론 테스트 | Raspberry Pi 5 |
| 하드웨어 제어 코드 개발·테스트 | Raspberry Pi 5 |
| 전체 통합 테스트 | Raspberry Pi 5 |

---

## 9. 시스템 흐름도

### 9.1 전체 동작 흐름

```
               ┌─────────────────────┐
               │    시스템 시작       │
               │ 모델 로드 / GPIO 초기화│
               └──────────┬──────────┘
                           │
              ┌────────────▼────────────┐
              │       SEARCH 상태        │◄──────────────────────┐
              │  제자리 회전, 녹색 LED   │                       │
              └────────────┬────────────┘                       │
                           │ 사람 감지 (2프레임 연속)            │
              ┌────────────▼────────────┐                       │
              │       WATCH 상태         │──15초 타임아웃────────►│
              │  회전 정지, 황색 점멸    │                       │
              │  PPE + Danger 분석      │◄──사람 소실────────────►│
              └────┬───────────────┬────┘                       │
                   │ 위험 감지      │ 위험 없음                  │
                   │               └───────────────────────────►│
              ┌────▼────────────────┐                           │
              │   ALERT_EVENT 상태   │                           │
              │   위반 유형 확정     │                           │
              └────────────┬────────┘                           │
                           │                                    │
              ┌────────────▼────────────┐                       │
              │      MOVE_TO 상태        │──20초 타임아웃────────►│
              │  bbox 기반 조향·이동     │──고착 감지(1.5초)─────►│
              │  황색 고속 점멸          │──사람 소실(1.5초)─────►│
              └────────────┬────────────┘                       │
                           │ 도착 (bbox 면적 ≥ 프레임 15%)       │
              ┌────────────▼────────────┐                       │
              │       ALERT 상태         │                       │
              │  음성 경고 + LED 강 점멸 │                       │
              │  스냅샷 + 로그 저장      │                       │
              └────────────┬────────────┘                       │
                           │ 30초 후                            │
                           └───────────────────────────────────►│
```

### 9.2 MOVE_TO 제어 루프 상세 (100ms 주기)

```
① person bbox 확인
   └── 5프레임 연속 없음 → SEARCH 전환

② 도착 판정
   └── bbox 면적 / 프레임 면적 ≥ 0.15 → 정지 → ALERT 전환

③ 고착 감지
   └── FORWARD 명령 중 15프레임 연속 bbox 면적 변화 < 500px²
       → 즉시 SEARCH 전환

④ 조향 결정 (①②③ 미해당 시)
   └── bbox 중심 X − 프레임 중심 X = 오차
       │  오차 > +50px → TURN_RIGHT (0.32)
       │  오차 < −50px → TURN_LEFT  (0.32)
       └  그 외         → FORWARD    (0.40)
```

### 9.3 PPE + 위험 표지 분석 흐름

```
WATCH 상태
    │
    ├── [Person Detector] → 사람 bbox 목록 (가장 큰 bbox를 타깃으로)
    │
    ├── [PPE Detector] → PPE 클래스 bbox 목록
    │       └── 각 PPE bbox × person bbox 포함 비율 계산
    │               ├── no-helmet 포함 비율 ≥ 50% → 해당 인물 미착용
    │               └── no-vest   포함 비율 ≥ 50% → 해당 인물 미착용
    │               3프레임 연속 시 → PPE_VIOLATION
    │
    └── [Danger Detector] → 위험 표지 bbox 목록
            └── person 중심 ↔ 표지 중심 정규화 거리 계산
                    < 2.0 이고 person 높이 ≥ 프레임 15%
                    → DANGER_ZONE_ALERT

    위반 발생 시 → ALERT_EVENT 전환
```

---

## 10. 개발 일정

### 10.1 팀 구성 및 역할

> **총 인원: 4명 / 총 기간: 2주 (10 영업일)**

| 멤버 | 역할 | 주요 담당 |
|------|------|---------|
| 멤버 A | AI 리드 | Person Detector 연동, PPE Detection 모델 학습·변환 |
| 멤버 B | AI / 데이터 | 데이터셋 수집·전처리, Danger Detection 모델 학습·변환 |
| 멤버 C | HW / 제어 | 로봇 조립, motor.py·output.py 구현, 자율 이동 로직 |
| 멤버 D | SW 통합 | 상태머신(main.py), detector.py 통합 파이프라인, 전체 통합·테스트 |

> Day 1~3: 전원 공통 (환경 구성 + HW 조립)
> Day 4~9: 역할 분배 (병렬 개발)
> Day 10: 전원 공통 (데모 준비 + 최종 시연)

---

### 10.2 일별 상세 일정

#### Day 1 (월) — 킥오프 & 환경 구성 `전원 공통`

| 항목 | 내용 |
|------|------|
| 오전 | 역할 분담 확정, GitHub 레포 생성, 브랜치 전략 수립 |
| 오후 | Pi5 OS 설치, SSH 접속, Python 가상환경 구성, 필수 라이브러리 설치 |
| 완료 기준 | 4명 전원 Pi5 SSH 접속 + GitHub push 성공 |

---

#### Day 2 (화) — 하드웨어 조립 `전원 공통`

| 항목 | 내용 |
|------|------|
| 오전 | 로봇 프레임 조립, 모터·L298N 배선 |
| 오후 | 카메라 모듈 장착, LED·스피커 배선, 전원 분리 확인 |
| 완료 기준 | 모터 수동 구동 확인, picamera2 프레임 캡처 확인 |

---

#### Day 3 (수) — 기초 HW 제어 검증 `전원 공통`

| 항목 | 내용 |
|------|------|
| 오전 | motor.py 구현 및 단위 테스트 (gpiozero 기반, 전진·정지·좌우 조향·제자리 회전, 중복 명령 방지 캐싱 검증) |
| 오후 | output.py LED + 스피커 테스트 |
| 완료 기준 | 모터 전 방향 제어 검증, LED 점멸·스피커 출력 확인 |

---

#### Day 4 (목) — 병렬 개발 시작

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A | Person Detector (YOLOv5n TFLite) Pi5 동작 확인, detector.py 기본 구조 작성 | Pi5에서 사람 bbox 출력 확인 |
| B | PPE 데이터셋 다운로드 (Kaggle HardHat-Vest v3 등), 클래스 라벨 확인 및 정제 | helmet / no-helmet / vest / no-vest 4클래스 데이터셋 준비 완료 |
| C | SEARCH 제자리 회전 구현, MOVE_TO bbox 기반 조향·고착 감지 로직 구현 | 제자리 회전 + 전진·조향 동작 확인 |
| D | main.py 상태머신 골격 구현 (전 상태 + 타임아웃) | 상태 전이 단위 테스트 통과 |

---

#### Day 5 (금) — AI 학습 & 제어 고도화

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A | PPE Detection 모델 학습 시작 (Colab), 데이터 증강 적용 | 학습 실행, 검증 mAP 추이 모니터링 |
| B | Danger Sign 데이터셋 수집·정제, Danger Detection 모델 학습 시작 | 위험 표지 데이터셋 준비 + 학습 실행 확인 |
| C | MOVE_TO 도착 판정 (면적 비율) + 고착 감지 + 사람 소실 처리 로직 완성 | 사람 bbox 추적하며 이동 후 정지 동작 확인 |
| D | detector.py 상태별 조건부 실행 구현, camera.py 연동 | 카메라 → Person 감지 → 이벤트 흐름 확인 |

---

#### Day 6 (월) — AI 변환 & 통합 준비

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A | PPE Detection 모델 TFLite INT8 변환, Pi5 추론 속도 실측 | Pi5에서 PPE bbox 출력 + 추론 시간 기록 |
| B | Danger Detection 모델 TFLite 변환, Pi5 동작 확인 | Pi5에서 위험 표지 bbox 출력 확인 |
| C | 자율 이동 전체 시나리오 테스트 (SEARCH → WATCH → MOVE_TO → ALERT), 타임아웃·고착 감지 동작 확인 | 전체 흐름 1회 이상 성공 |
| D | IoU 기반 PPE 착용 판정 로직 구현 (detector.py 완성), logger.py 구현 | PPE 위반 이벤트 발생 확인 |

---

#### Day 7 (화) — 전체 파이프라인 통합

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A | PPE + Person 파이프라인 통합 테스트, IoU 임계값 조정 | PPE 위반 → ALERT_EVENT 정상 발생 |
| B | Danger + 근접 판정 통합 테스트, 거리 임계값 조정 | 위험 표지 근접 → ALERT_EVENT 정상 발생 |
| C | output.py + 상태머신 연동 (LED 상태별 점멸, 음성 경고) | 이벤트 수신 → 음성 + LED 경고 동작 |
| D | main.py 전체 파이프라인 통합, 타임아웃 동작 검증 | main.py 실행 시 전체 상태 흐름 동작 확인 |

---

#### Day 8 (수) — 전체 통합 테스트 1차

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A + B | AI 정확도 미달 시 임계값 조정·재학습, 오감지 케이스 분석 | 시나리오 오감지 ≤ 2회/10회 |
| C | 자율 이동 안정성 테스트 (조향·도착·고착 감지 반복), 배선 점검 | 이동 시나리오 10회 반복 안정 동작 |
| D | 타임아웃·상태 전이 엣지케이스 처리, 로그 저장 확인 | 시나리오 5회 중 4회 이상 성공 |

---

#### Day 9 (목) — 최종 통합 테스트 & 안정화

| 멤버 | 작업 | 완료 기준 |
|------|------|---------|
| A + B | 최종 모델 확정, config.yaml 임계값 최적화 | PPE / Danger mAP ≥ 70% 확인 |
| C + D | 데모 시나리오 3회 완주 테스트, 배터리 소모 측정 | 시나리오 3회 모두 정상 동작 |
| 전원 | 코드 정리 (주석, README), GitHub 최종 커밋 | 코드 리뷰 및 문서 정리 완료 |

---

#### Day 10 (금) — 발표 준비 & 최종 시연 `전원 공통`

| 항목 | 내용 |
|------|------|
| 오전 | 발표 자료 제작 (시연 영상 촬영 포함), 데모 리허설 |
| 오후 | 최종 시연 발표 |
| 완료 기준 | 발표 자료 완성, 데모 시연 성공 |

---

### 10.3 일정 리스크 완충 전략

| 리스크 발생 시점 | 상황 | 대응 |
|----------------|------|------|
| Day 5 종료 | PPE mAP 미달 | 임계값 조정 후 진행, 재학습은 Day 6 병행 |
| Day 7 종료 | 통합 미완성 | F-06 (위험 표지) 제외, PPE 기능만으로 데모 |
| Day 8 종료 | 자율 이동 불안정 | MOVE_TO 제거, WATCH 상태에서 제자리 경고 방식으로 대체 |

---

## 11. 리스크 및 대응 방안

| 리스크 | 확률 | 영향 | 대응 방안 |
|--------|------|------|---------|
| Pi5 추론 속도 부족 | 중 | 고 | 입력 해상도 축소, WATCH 상태 fps 낮춤, 실측 후 최적화 |
| PPE Detection 정확도 미달 | 중 | 중 | IoU 임계값 조정, 데이터 증강 강화, 재학습 |
| 자율 이동 조향 불안정 | 중 | 중 | center_margin 조정 (50 → 70px), 속도 낮춤 |
| MOVE_TO 중 장애물 충돌 | 중 | 고 | 데모 환경 내 장애물 제거, 고착 감지로 조기 탈출 |
| gpiozero 핀 설정 오류 | 낮 | 고 | Day 2~3 모터 단독 테스트로 배선 선행 검증 |
| 조명 변화 → AI 정확도 저하 | 중 | 중 | 다양한 조명 환경 자체 촬영 데이터 추가 |
| 개발 일정 지연 | 중 | 중 | Day 8~9 완충 일정, Must 기능 우선 완성 원칙 |

---

## 12. 성능 지표 (KPI)

| 지표 | 목표값 | 측정 방법 |
|------|--------|---------|
| PPE Detection mAP | ≥ 70% | 검증 데이터셋 평가 |
| 위험 표지 Detection mAP | ≥ 70% | 검증 데이터셋 평가 |
| 감지 → 경고 발령 지연 | ≤ 3초 | 스톱워치 측정 |
| 추론 처리 주기 (SEARCH) | ≥ 6fps | 코드 타이머 실측 |
| 추론 처리 주기 (WATCH) | ≥ 2fps | 코드 타이머 실측 |
| MOVE_TO 도착 성공률 | ≥ 80% | 10회 반복 테스트 |
| 고착 감지 반응 시간 | ≤ 2초 | 장애물 설치 후 SEARCH 전환까지 측정 |
| 데모 시나리오 성공률 | ≥ 80% | 5회 반복 시나리오 |
| 오경보율 (False Positive) | ≤ 20% | 실환경 10회 테스트 |

---

## 부록 A. config.yaml

```yaml
camera:
  width: 320
  height: 240
  fps: 30
  inference_fps: 3

models:
  person_detector: models/person_detect.tflite
  ppe_detector:    models/ppe_detect.tflite
  danger_detector: models/danger_detect.tflite

thresholds:
  person_confidence:        0.5
  ppe_confidence:           0.5
  danger_confidence:        0.5
  ppe_iou_ratio:            0.5    # PPE bbox의 person bbox 내 포함 비율
  danger_proximity_norm:    2.0    # 사람 키 기준 정규화 거리 임계값
  arrival_area_ratio:       0.15   # bbox 면적 / 프레임 면적 도착 판정 기준
  person_min_height_ratio:  0.15   # danger 판정 최소 사람 크기 (프레임 높이 대비)
  ppe_consecutive_frames:   3      # PPE 위반 확정 연속 프레임 수
  person_lost_frames:       5      # 사람 소실 판정 연속 프레임 수
  stuck_check_frames:       15     # 고착 감지 관찰 프레임 수 (약 1.5초)
  stuck_area_delta:         500    # 고착 판정 bbox 면적 변화 임계값 (px²)

state_timeout:
  WATCH:   15    # 초
  MOVE_TO: 20
  ALERT:   30

alert:
  volume: 0.8

motor:
  left_in1:      17
  left_in2:      27
  left_pwm:      18
  right_in1:     22
  right_in2:     23
  right_pwm:     13
  forward_speed: 0.40
  turn_speed:    0.32
  search_speed:  0.25
  center_margin: 50        # 조향 판정 오차 범위 (px)

led:
  pin:  12
  mode: pwm
```

---

## 부록 B. 데모 시나리오

**시나리오 1 — 안전모 미착용 감지 및 접근 경고**

1. 로봇 전원 ON → SEARCH 상태 (제자리 회전, 녹색 LED)
2. 안전모 미착용 작업자가 카메라 시야에 진입
3. 2프레임 연속 감지 → WATCH 전환 (회전 정지, 황색 점멸 1Hz)
4. PPE Detector → `no-helmet` bbox 감지, person bbox IoU ≥ 50% 확인
5. 3프레임 연속 확인 → PPE_VIOLATION → ALERT_EVENT
6. MOVE_TO: 작업자를 향해 이동 (황색 점멸 2Hz)
7. bbox 면적 ≥ 프레임 15% → 도착 → ALERT
8. 음성 "안전모를 착용하세요" + 황색 고속 점멸 (3Hz)
9. 30초 후 → SEARCH 복귀

**시나리오 2 — 위험 구역 근접 감지 및 접근 경고**

1. 로봇 SEARCH 상태에서 위험 표지 근처 작업자 탐색
2. 사람 감지 → WATCH 전환
3. Danger Detector → 위험 표지 bbox 감지, 정규화 거리 < 2.0 + person 크기 ≥ 15% 확인
4. DANGER_ZONE_ALERT → ALERT_EVENT → MOVE_TO
5. 도착 → ALERT: 음성 "위험 구역입니다. 즉시 대피하세요" + 적색 고속 점멸 (4Hz)
6. 30초 후 → SEARCH 복귀

---

## 부록 C. 참고 자료

- TensorFlow Lite 공식 문서: https://www.tensorflow.org/lite/guide
- Raspberry Pi picamera2: https://github.com/raspberrypi/picamera2
- YOLOv5 TFLite Export: https://github.com/ultralytics/yolov5
- Kaggle HardHat-Vest Dataset v3: https://www.kaggle.com/datasets/muhammetzahitaydn/hardhat-vest-dataset-v3
- Kaggle Construction Site Safety: https://www.kaggle.com/datasets/snehilsanyal/construction-site-safety-image-dataset-roboflow
- Roboflow PPE Universe: https://universe.roboflow.com/search?q=class:helmet+and+vest
- gpiozero 공식 문서: https://gpiozero.readthedocs.io

---

*On-Device AI 프로젝트 기획서 v4.1  |  AI 로봇 SW 개발자 교육과정*
